#include <bits/stdc++.h>
using namespace std;

int main() {
  while (true) {
    string mode;
    cin >> mode;
    if (mode == "Quit" || mode == "Exit") {
      cout << "Good bye!" << endl;
      break;
    } else{
      cout << mode << ":" << endl;
    }

  }

  return 0;
}
